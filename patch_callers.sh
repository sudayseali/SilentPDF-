sed -i 's/ListPdfItem(/ListPdfItem(\n                                    pdf = pdf,\n                                    onClick = {\n                                        viewModel.openPdf(pdf)\n                                        onNavigateToReader()\n                                    },\n                                    onFavoriteToggle = { viewModel.toggleFavorite(pdf) },\n                                    onShare = { sharePdf(context, pdf) },\n                                    onDelete = { viewModel.deletePdf(pdf) }\n                                )/g' app/src/main/java/com/example/ui/screens/LibraryScreen.kt

sed -i 's/GridPdfItem(/GridPdfItem(\n                                    pdf = pdf,\n                                    onClick = {\n                                        viewModel.openPdf(pdf)\n                                        onNavigateToReader()\n                                    },\n                                    onFavoriteToggle = { viewModel.toggleFavorite(pdf) },\n                                    onShare = { sharePdf(context, pdf) },\n                                    onDelete = { viewModel.deletePdf(pdf) }\n                                )/g' app/src/main/java/com/example/ui/screens/LibraryScreen.kt
